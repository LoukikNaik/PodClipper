# Ubiquitous Language

Source of truth for every named thing in this repo. Look it up before naming or renaming anything. If you introduce a new concept, add the row here in the same commit. If you rename in code, rename here too.


| Term                                                                                                                                         | Definition                                                                                                          |
| -------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| `.cache/<stem>-<hash>/`                                                                                                                      | Per-source-video cache directory; hash is sha1 of resolved absolute path, truncated to 10 chars.                    |
| `01_raw_picks.json`                                                                                                                          | Analyze debug cache: LLM's raw response + parsed clip list before any snapping.                                     |
| `02_snapped_and_capped.json`                                                                                                                 | Analyze debug cache: clips after segment-boundary snap and target-count cap.                                        |
| `03_refined.json`                                                                                                                            | Analyze debug cache: per-clip refiner traces + clips after word-precise refinement.                                 |
| `04_final.json`                                                                                                                              | Analyze debug cache: clips after title rewrites — what the pipeline actually produces.                              |
| `analyze/`                                                                                                                                   | Subdir of `.cache/<video>/` holding per-stage JSON snapshots from `analyze_for_reels`.                              |
| `analyze_for_reels`                                                                                                                          | Stage 4: sends the timestamped transcript to an LLM and returns a list of `Clip` objects.                           |
| `anthropic_api`                                                                                                                              | LLM provider that calls Claude via the official `anthropic` SDK; requires `ANTHROPIC_API_KEY`.                      |
| `apply_min_dwell`                                                                                                                            | DEPRECATED: merges short timeline segments into the previous one (`crop.mode: single` only).                        |
| `arc`                                                                                                                                        | Reel scorecard axis: clip tells a complete setup → development → payoff story (1–5).                                |
| `audio.wav`                                                                                                                                  | Cached whole-video mono 16kHz WAV at the root of `.cache/<video>/`.                                                 |
| `auto`                                                                                                                                       | Default `cfg.crop.mode`: shot-aware renderer (single panel for close-ups, stacked for wide shots).                  |
| `back-of-head`                                                                                                                               | A YOLO person bbox with no front-facing face detected; deprioritized when picking primary.                          |
| `BBox`                                                                                                                                       | Dataclass: `(x, y, w, h, confidence)` with `x_center`/`y_center`/`area`/`iou` helpers.                              |
| `blaze_face_short_range.tflite`                                                                                                              | MediaPipe short-range face detector model, cached under `cfg.paths.cache_dir`.                                      |
| `body-IoU lock`                                                                                                                              | Crop bbox stays locked while the YOLO body-bbox IoU vs. last anchor ≥ `stacked_iou_threshold`.                      |
| `build_provider`                                                                                                                             | Factory in `src/llm/__init__.py` that instantiates the provider named by `cfg.llm.provider`.                        |
| `build_speaker_timeline`                                                                                                                     | DEPRECATED: turns per-frame bboxes (+ optional diarization) into a Timeline.                                        |
| `build_trailer_words`                                                                                                                        | Remaps each pick's clip-local 2nd-pass words into trailer-relative timestamps.                                      |
| `burn_subtitles`                                                                                                                             | Renders karaoke word-highlight subtitles + optional fading title overlay onto a video.                              |
| `cache_dir`                                                                                                                                  | `cfg.paths.cache_dir` — root for all intermediate artifacts; default `.cache`.                                      |
| `ChunkRange`                                                                                                                                 | Dataclass for one parallel-transcription chunk: `(index, start, end)`.                                              |
| `claude_cli`                                                                                                                                 | Default LLM provider; shells out to the `claude` binary via `claude -p`.                                            |
| `classify_wide_shot_frames`                                                                                                                  | Active wide-shot classifier; returns per-frame bool array from bbox geometry.                                       |
| `cleanup_words`                                                                                                                              | LLM post-pass that fixes mis-spellings and transliterates non-Latin scripts.                                        |
| `Clip`                                                                                                                                       | Dataclass: one reel-worthy moment with `(start, end, title, reason, hook_score)`.                                   |
| `ClipArtifacts`                                                                                                                              | Aggregates everything produced for a single clip through the per-clip pipeline.                                     |
| `closer_punch`                                                                                                                               | Trailer scorecard axis: last pick lands as a memorable closer (1–5).                                                |
| `compute_tech_metrics`                                                                                                                       | Computes objective `TechMetrics` from pipeline data (face vis, crop stability, wps, etc.).                          |
| `concat_with_black_gaps`                                                                                                                     | ffmpeg concat-filter wrapper that splices cropped pick clips with black + silent gaps.                              |
| `ContentScores`                                                                                                                              | Dataclass holding the 6 reel-evaluator dimensions + overall/verdict/feedback.                                       |
| `cropped.mp4`                                                                                                                                | Per-clip cache artifact: the cropped video before subtitles are burned.                                             |
| `cropped_regen.mp4`                                                                                                                          | Per-clip cache artifact written by `regen_crops.py` (avoids overwriting `cropped.mp4`).                             |
| `cut_segment`                                                                                                                                | Trailer ffmpeg cut for one pick's [cut_start, cut_end] window.                                                      |
| `debug_cache_dir`                                                                                                                            | Optional kwarg to `analyze_for_reels` enabling the `analyze/` per-stage JSON dump.                                  |
| `debug_detect.mp4`                                                                                                                           | Detect-stage debug overlay: per-person bboxes colored by front/back face attribution.                               |
| `debug_mouth.mp4`                                                                                                                            | Diarize-stage debug overlay: mouth-region boxes colored by openness (deprecated path).                              |
| `derive_cut_bounds`                                                                                                                          | Trailer mechanical fallback bounds when `refine_cut_bounds_with_llm` fails.                                         |
| `detect_humans_all_per_frame`                                                                                                                | Active multi-person detection: returns ALL person bboxes per frame + face-attribution flags.                        |
| `detect_humans_per_frame`                                                                                                                    | DEPRECATED: returns one primary bbox per frame (loses multi-person info).                                           |
| `DiarSegment`                                                                                                                                | Dataclass: one speaker-active window `(start, end, speaker_id)` from pyannote.                                      |
| `diarize_clip`                                                                                                                               | DEPRECATED: pyannote.audio call returning `DiarSegment`s for a clip.                                                |
| `ending`                                                                                                                                     | Reel scorecard axis: final sentence lands as a satisfying close (1–5).                                              |
| `evaluate_content`                                                                                                                           | LLM-as-judge for a reel: returns `ContentScores` with 6-axis rubric.                                                |
| `evaluate_reel`                                                                                                                              | Combines tech metrics + content evaluation into a `ReelScorecard`.                                                  |
| `evaluate_trailer`                                                                                                                           | LLM-as-judge for trailer mode: returns `TrailerScorecard` with 5-axis rubric.                                       |
| `extract_audio`                                                                                                                              | Stage 2: ffmpeg-extract mono audio at `cfg.audio.sample_rate` into the cache.                                       |
| `face_aware`                                                                                                                                 | `cfg.detect.face_aware`: when true, anchors crop x on face center instead of body center.                           |
| `face_attribution`                                                                                                                           | Greedy assignment of detected faces to person bboxes; prevents neighbor-face leakage.                               |
| `face_landmarker.task`                                                                                                                       | MediaPipe FaceLandmarker model (lip/face landmarks) used by the diarize path.                                       |
| `face_per_person`                                                                                                                            | Output of `_attribute_faces_to_persons`: `face_bbox_or_None` aligned with input persons.                            |
| `fade_out_seconds`                                                                                                                           | `cfg.subtitles.fade_out_seconds`: trailing video-to-black + audio fade duration.                                    |
| `first_pass_transcript.json`                                                                                                                 | Cached `Transcript` from the first-pass Whisper run; reused on subsequent runs.                                     |
| `first pass`                                                                                                                                 | Whisper run #1: parallel-chunked fast transcription over the whole video for LLM clip selection.                    |
| `follow-the-editor`                                                                                                                          | Design philosophy of `auto` mode — mirror the source's shot cuts rather than override them.                         |
| `follow-the-speaker`                                                                                                                         | DEPRECATED design philosophy of `single` mode — diarization + cluster tracking.                                     |
| `front-face preference`                                                                                                                      | When picking primary, prefer persons whose face was detected over back-of-head candidates.                          |
| `full_transcript.json`                                                                                                                       | Cached trailer-mode first-pass `Transcript` under `.cache/<video>/trailer/`.                                        |
| `gap_seconds`                                                                                                                                | `cfg.trailer.gap_seconds`: duration of the black + silent gap between trailer picks.                                |
| `generate_subtitle_lines`                                                                                                                    | Groups raw words into screen-sized `SubLine`s using max-chars + sentence-end + pause rules.                         |
| `gradient backdrop`                                                                                                                          | Default title-overlay backdrop: dark-to-transparent sweep across the top 35% of the frame.                          |
| `head_h`                                                                                                                                     | Anchor: pixel distance from nose to shoulder-mid; drives the 1.5/3.5 panel-top/bottom multipliers.                  |
| `head_pad`                                                                                                                                   | `cfg.trailer.head_pad`: pre-roll buffer before the first spoken word of a trailer cut.                              |
| `height_cap_frac`                                                                                                                            | `cfg.crop.shot_height_cap_frac`: a person bbox must be shorter than this fraction to count for wide-shot.           |
| `hook`                                                                                                                                       | Reel scorecard axis: first 2 seconds stop the scroll (1–5).                                                         |
| `hook_score`                                                                                                                                 | LLM's self-reported confidence/ranking on a `Clip`; used to sort + cap to `target_clips`.                           |
| `ingest`                                                                                                                                     | Stage 1: ffprobe wrapper validating input and returning `VideoMeta`.                                                |
| `IngestError` / `AudioError` / `DetectError` / `CropError` / `SubtitleError` / `AnalyzeError` / `LLMError` / `DiarizeError` / `CleanupError` | Per-stage exception classes; pipeline catches at the per-clip boundary.                                             |
| `is_wide`                                                                                                                                    | Per-frame bool array from `classify_wide_shot_frames`; drives single ↔ stacked layout choice.                       |
| `karaoke subtitles`                                                                                                                          | Active-word highlighting (yellow) over inactive words (white), word-timed via Whisper 2nd pass.                     |
| `lerp transition`                                                                                                                            | When the body-IoU lock breaks, the rendered crop bbox eases toward the new target over `stacked_transition_frames`. |
| `link_timeline`                                                                                                                              | DEPRECATED: ties pyannote speakers to bbox clusters via mouth-motion variance.                                      |
| `LLMProvider`                                                                                                                                | Protocol exposing `complete(user_prompt, system_prompt, max_tokens) -> str`.                                        |
| `max_clip_seconds`                                                                                                                           | `cfg.analyze.max_clip_seconds`: upper bound on accepted clip duration.                                              |
| `min_clip_seconds`                                                                                                                           | `cfg.analyze.min_clip_seconds`: lower bound on accepted clip duration.                                              |
| `min_segment_dwell_seconds`                                                                                                                  | DEPRECATED: minimum timeline-segment length for `apply_min_dwell`.                                                  |
| `miss tolerance`                                                                                                                             | `cfg.crop.stacked_miss_tolerance`: bridge up to N consecutive YOLO/Pose dropouts before resetting the lock.         |
| `mode`                                                                                                                                       | CLI flag: `reels` (default — N independent clips) or `trailer` (one spliced trailer).                               |
| `mouth_openness`                                                                                                                             | Mouth-gap / face-height ratio at one frame; deprecated speaker-linking signal.                                      |
| `multi-shot timeline`                                                                                                                        | DEPRECATED: emits one timeline segment per cluster-active window (`_multi_shot_timeline`).                          |
| `name`                                                                                                                                       | Attribute on every `LLMProvider`; used in log lines and the build factory.                                          |
| `opener_hook`                                                                                                                                | Trailer scorecard axis: first pick lands as a viral hook (1–5).                                                     |
| `outputs/<timestamp>/`                                                                                                                       | Final reel destination; one timestamped dir per pipeline run.                                                       |
| `pacing`                                                                                                                                     | Trailer scorecard axis: cuts breathe but the whole thing has rhythm (1–5).                                          |
| `pad_seconds`                                                                                                                                | `cfg.clip_extract.pad_seconds`: ffmpeg cut buffer around (refined) clip bounds; tiny (0.15s).                       |
| `panel_aspect`                                                                                                                               | Aspect ratio of a stacked panel (`9:8`) vs. a single full reel (`9:16`).                                            |
| `per_frame_persons`                                                                                                                          | `list[list[BBox]]` from `detect_humans_all_per_frame` — all persons per frame.                                      |
| `Pick`                                                                                                                                       | A trailer-mode quotable sentence dict with `(start, end, sentence, cut_start, cut_end, refined_sentence)`.          |
| `pick_quotables`                                                                                                                             | Trailer LLM #1: picks 4–5 quotable sentences from the full transcript.                                              |
| `pill backdrop`                                                                                                                              | Legacy alternative to gradient backdrop: rounded rectangle behind the title text.                                   |
| `plan_chunks`                                                                                                                                | Splits `[0, duration]` into overlapping `ChunkRange`s for the first-pass transcribe.                                |
| `pose anchor`                                                                                                                                | `(nose, shoulder_mid)` source-pixel pair from MediaPipe Pose Landmarker; drives panel framing.                      |
| `pose_landmarker_lite.task`                                                                                                                  | MediaPipe Pose Landmarker lite model, cached under `cfg.paths.cache_dir`.                                           |
| `primary`                                                                                                                                    | The single chosen person bbox per frame (legacy path) — biggest, or face-aware-preferred.                           |
| `publish`                                                                                                                                    | Reel verdict: overall score ≥ `cfg.evaluate.publish_threshold` (default 4.0).                                       |
| `q_NN_segment.mp4`                                                                                                                           | Trailer cache: the ffmpeg-cut video for pick N.                                                                     |
| `q_NN_words.json`                                                                                                                            | Trailer cache: cached 2nd-pass Whisper words for pick N.                                                            |
| `q_NN_cropped.mp4`                                                                                                                           | Trailer cache: shot-aware-cropped version of pick N.                                                                |
| `quotables.json`                                                                                                                             | Cached trailer LLM #1 output (`pick_quotables`) under `.cache/<video>/trailer/`.                                    |
| `raw wide`                                                                                                                                   | Per-frame wide-shot decision before temporal smoothing by `shot_smooth_window_frames`.                              |
| `Reel`                                                                                                                                       | One produced 9:16 short-form video — output of reels-mode pipeline.                                                 |
| `reel_NN_<slug>.mp4`                                                                                                                         | Final reel naming convention; slug is derived from the title (lowercase, dash-separated, ≤40 chars).                |
| `reel_NN_<slug>.txt`                                                                                                                         | Sidecar with title, reason, source bounds, hook_score, and the full evaluation scorecard.                           |
| `reel_detector.txt`                                                                                                                          | Selection prompt: tells the LLM what makes a reel-worthy clip.                                                      |
| `reel_refiner.txt`                                                                                                                           | Word-precise bounds-refinement prompt; LLM picks `first_word_idx`/`last_word_idx`.                                  |
| `ReelScorecard`                                                                                                                              | Dataclass combining `TechMetrics` + `ContentScores` into a single verdict.                                          |
| `refine_bounds`                                                                                                                              | `cfg.analyze.refine_bounds`: enables the per-clip LLM bounds-refinement pass.                                       |
| `refine_clip_bounds_with_llm`                                                                                                                | Per-clip refinement: LLM picks word-precise first/last indices over a word list with context.                       |
| `refine_cut_bounds_with_llm`                                                                                                                 | Trailer LLM #2: per-pick boundary refinement returning `(cut_start, cut_end, refined_sentence)`.                    |
| `refined_bounds.json`                                                                                                                        | Cached trailer refiner output under `.cache/<video>/trailer/`.                                                      |
| `refiner_window_s`                                                                                                                           | `cfg.analyze.refiner_window_s` / `cfg.trailer.refiner_window_s`: ±seconds of word context shown to refiner.         |
| `regen_crops.py`                                                                                                                             | Script that re-renders cached reels with the latest crop logic (skips LLM + audio + 1st-pass).                      |
| `review`                                                                                                                                     | Reel verdict: overall content score between `skip_threshold` and `publish_threshold`.                               |
| `run_pipeline`                                                                                                                               | Entry point for reels mode (`--mode reels`), in `src/pipeline.py`.                                                  |
| `run_trailer_pipeline`                                                                                                                       | Entry point for trailer mode (`--mode trailer`), in `src/pipeline.py`.                                              |
| `second pass`                                                                                                                                | Whisper run #2: single-shot high-quality re-transcription per clip; produces subtitle word timings.                 |
| `segment.mp4`                                                                                                                                | Per-clip cache artifact: raw ffmpeg-extracted clip segment with `pad_seconds` buffer.                               |
| `shareability`                                                                                                                               | Reel scorecard axis: likelihood of share / save / comment (1–5).                                                    |
| `shot-aware crop`                                                                                                                            | Default crop path: chooses single 9:16 panel for close-ups, stacked dual 9:8 panels for wide shots.                 |
| `shot_sep_frac`                                                                                                                              | `cfg.crop.shot_sep_frac`: leftmost/rightmost qualifying bboxes must be ≥ this fraction of width apart.              |
| `shot_smooth_window_frames`                                                                                                                  | `cfg.crop.shot_smooth_window_frames`: window size for `is_wide` majority smoothing.                                 |
| `sidecar`                                                                                                                                    | Plain-text `.txt` next to each reel/trailer with metadata + scorecard.                                              |
| `single`                                                                                                                                     | (1) DEPRECATED `cfg.crop.mode` (legacy follow-the-speaker). (2) Non-wide frame layout in `auto` mode.               |
| `skip`                                                                                                                                       | Reel verdict: overall content score below `cfg.evaluate.skip_threshold` (default 3.0).                              |
| `skipped/`                                                                                                                                   | Subdir of the run output where auto-skipped reels are moved when `evaluate.auto_skip = true`.                       |
| `smart_crop_916`                                                                                                                             | DEPRECATED single-panel timeline-driven 9:16 renderer (`crop.mode: single`).                                        |
| `smart_crop_916_stacked`                                                                                                                     | Active shot-aware renderer with single ↔ stacked-dual-panel layout.                                                 |
| `snap-to-target`                                                                                                                             | Once the rendered bbox is within 1 px of target on every axis, snap exactly (no wobble).                            |
| `stacked panel`                                                                                                                              | One of two 9:8 vertical panels in wide-shot layout, tightly framed on one person's face+shoulders.                  |
| `stacked_iou_threshold`                                                                                                                      | `cfg.crop.stacked_iou_threshold`: body-bbox IoU below which the crop lock breaks (0.70).                            |
| `stacked_miss_tolerance`                                                                                                                     | See `miss tolerance`.                                                                                               |
| `stacked_snap_px`                                                                                                                            | `cfg.crop.stacked_snap_px`: crop dims are snapped to multiples of this to suppress sub-pixel drift.                 |
| `stacked_top_is_right`                                                                                                                       | `cfg.crop.stacked_top_is_right`: when true, the rightmost person goes in the top panel (default false).             |
| `stacked_transition_frames`                                                                                                                  | `cfg.crop.stacked_transition_frames`: how many frames the rendered crop lerps over after a lock-break.              |
| `standalone`                                                                                                                                 | Reel scorecard axis: comprehensible to a stranger without prior context (1–5).                                      |
| `standalone_quality`                                                                                                                         | Trailer scorecard axis: a viewer with no prior context still gets it (1–5).                                         |
| `stitched.mp4`                                                                                                                               | Trailer cache: output of `concat_with_black_gaps` before subtitles burned.                                          |
| `SubLine`                                                                                                                                    | Dataclass for one rendered subtitle line: `(start, end, words)`.                                                    |
| `tail_pad`                                                                                                                                   | `cfg.trailer.tail_pad`: post-roll buffer after the last spoken word of a trailer cut.                               |
| `target_clips`                                                                                                                               | `cfg.analyze.target_clips`: hint to the LLM for how many reels to return.                                           |
| `TechMetrics`                                                                                                                                | Dataclass: objective per-reel quality numbers (face_visibility, crop_stability, words_per_second, …).               |
| `thematic_coherence`                                                                                                                         | Trailer scorecard axis: picks form one consistent thread (1–5).                                                     |
| `Timeline`                                                                                                                                   | Type alias `list[TimelineSegment]`; consumed by the legacy `smart_crop_916` renderer.                               |
| `TimelineSegment`                                                                                                                            | Dataclass: `(start, end, label, bbox_at)` where `bbox_at(frame_idx) -> BBox                                         |
| `title_match`                                                                                                                                | Reel scorecard axis: title represents the content + creates the right curiosity gap (1–5).                          |
| `title_overlay`                                                                                                                              | Fading top-of-frame title block; styled via `cfg.subtitles.title_overlay.*`.                                        |
| `Trailer`                                                                                                                                    | One spliced short-form video built from 4–5 quotable picks with black-frame transitions.                            |
| `trailer.mp4` / `trailer.txt`                                                                                                                | Final trailer file + sidecar in `outputs/<timestamp>/`.                                                             |
| `trailer_evaluator.txt`                                                                                                                      | Prompt for `evaluate_trailer`; 5-axis judge.                                                                        |
| `trailer_picks.txt`                                                                                                                          | Prompt for `pick_quotables`; LLM #1 of trailer mode.                                                                |
| `trailer_refiner.txt`                                                                                                                        | Prompt for `refine_cut_bounds_with_llm`; LLM #2 of trailer mode.                                                    |
| `TrailerScorecard`                                                                                                                           | Dataclass holding the 5 trailer-evaluator dimensions + overall/verdict/feedback.                                    |
| `transcribe_first_pass`                                                                                                                      | Stage 3a: parallel-chunked Whisper pass over the whole audio.                                                       |
| `transcribe_second_pass`                                                                                                                     | High-quality per-clip Whisper pass returning word-level timestamps.                                                 |
| `transcribe_second_pass_cached`                                                                                                              | Same but reads/writes `words.json` so repeated runs are instant.                                                    |
| `Transcript`                                                                                                                                 | Dataclass: `(language, segments[])` with a `full_text` property.                                                    |
| `TranscriptSegment`                                                                                                                          | One Whisper segment: `(start, end, text, words[])`.                                                                 |
| `transcript_to_timestamped_text`                                                                                                             | Compact `[mm:ss-mm:ss] text` formatting consumed by the selector LLM.                                               |
| `transliterate`                                                                                                                              | Cleanup-pass behavior: rewrite Devanagari/Urdu/Arabic/etc. tokens into Latin phonetic spellings.                    |
| `verdict`                                                                                                                                    | Final reel/trailer label: `publish`                                                                                 |
| `VideoMeta`                                                                                                                                  | Dataclass returned by `ingest`: `(path, duration, width, height, fps, has_audio, codec)`.                           |
| `wide`                                                                                                                                       | Per-frame shot type meaning "two visible people, both shorter than the height cap, separated enough."               |
| `wide-shot classifier`                                                                                                                       | `classify_wide_shot_frames`; pure bbox geometry, temporally smoothed.                                               |
| `Word`                                                                                                                                       | Dataclass: `(start, end, text, confidence)` — Whisper word with timestamps.                                         |
| `words.json`                                                                                                                                 | Per-clip cache artifact: serialized 2nd-pass `Word` list (subtitles' source of truth).                              |
| `yolov8n.pt`                                                                                                                                 | YOLOv8 nano weights, auto-downloaded by ultralytics on first run; sits in repo root.                                |


