faster-whisper>=1.1.0
ultralytics>=8.3.0
opencv-python>=4.10
numpy>=1.26
Pillow>=10.0
anthropic>=0.42.0
litellm>=1.50              # unified LLM gateway (Anthropic, OpenAI, Gemini, Groq, Ollama, ...)
pyyaml>=6.0
rich>=13.0

# Multi-speaker diarization (single-camera interviews)
pyannote.audio>=3.1        # requires HF_TOKEN — accept terms at
                           # https://huggingface.co/pyannote/speaker-diarization-3.1
mediapipe>=0.10            # face landmarks for mouth-motion speaker linking
torch>=2.0                 # pulled in by pyannote
