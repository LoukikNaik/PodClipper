"""Shared pytest fixtures for the characterization suite.

Add fixtures here that need to be shared across multiple test modules
(e.g. a minimal `cfg` SimpleNamespace, a tmp cache dir, etc.). Keep
fixtures small and obvious — characterization tests rely on transparency.
"""
